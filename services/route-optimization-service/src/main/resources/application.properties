spring:
  data:
    mongodb:
      uri: ${MONGO_URI}
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}
kafka:
  broker: ${KAFKA_BROKER}
  apiKey: ${CONFLUENT_API_KEY}
  apiSecret: ${CONFLUENT_API_SECRET}

server:
  port: 8090